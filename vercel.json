{
  "buildCommand": "cd frontend && npm install && npm run build && mv build ../build",
  "outputDirectory": "build",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}